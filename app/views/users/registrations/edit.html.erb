<%= render 'layouts/users_header'%>

<body class="login-body" style="height: 1024px;">

  <%= render "devise/shared/error_messages", resource: resource %>
  <div id="login", style="height: 700px;">
      <h1 class="log-title">Edit User</h1>

<%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>

  <div class="field-login">
    <%= f.label :ユーザーネーム %><br />
    <%= f.text_field :name, autofocus: true, autocomplete: "name", class:"form-control" %>
  </div>

  <div class="field-login">
    <%= f.label :メールアドレス %><br />
    <%= f.email_field :email, autofocus: true, autocomplete: "email", class:"form-control" %>
  </div>

  <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
    <div>Currently waiting confirmation for: <%= resource.unconfirmed_email %></div>
  <% end %>

  <div class="field-login">
    <%= f.label :新しいパスワード %> <i>（変更しない場合は空欄）</i><br />
    <%= f.password_field :password, autocomplete: "new-password", class:"form-control" %>
  </div>

  <div class="field-login">
    <%= f.label :パスワード確認 %><br />
    <%= f.password_field :password_confirmation, autocomplete: "new-password", class:"form-control" %>
  </div>

  <div class="field-login">
    <%= f.label :現在のパスワード %> <br><i style="font-size: 12px;">（上記の情報を変更するには現在のパスワードが必要です）</i><br />
    <%= f.password_field :current_password, autocomplete: "current-password", class:"form-control" %>
  </div>

  <div class="actions">
    <%= f.submit "更新", class: "signup_btn" %>
  </div>
<% end %>
<br>
<h4>退会</h4>

<p>アカウントを削除すると復元はできません。<br>本当に退会しますか？ <%= button_to "退会する", registration_path(resource_name), data: { confirm: "退会しますか？" }, method: :delete, class: "btn btn-default btn-sm" %></p>

  </div>
</body>